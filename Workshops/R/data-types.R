x<-3 #define x as numeric
x<-1+2i #define x as complex
x<-'a' #as character
x<-"asd" #still same
x<-TRUE #boolean now

is.numeric(x) #returns BOOLEAN
x<-as.integer(x) #type casting x to integer
class(x) #returns the class of variable
print(x) #prints the arguments
